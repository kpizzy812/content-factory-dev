<script setup lang="ts">
/**
 * Нет прав на раздел.
 *
 * Важная деталь для оператора: пункта нет в сайдбаре вообще, поэтому сюда
 * можно попасть только по прямой ссылке. Без этого объяснения человек решает,
 * что интерфейс сломался.
 */
defineProps<{
  section?: string
}>()
</script>

<template>
  <UiEmptyState
    variant="denied"
    title="Нет доступа к разделу"
    :description="`${section ? `Раздел «${section}» ` : 'Этот раздел '}недоступен с вашими правами и не показывается в навигации — вы открыли его по прямой ссылке. Доступ выдаёт администратор.`"
  >
    <UiButton @click="navigateTo('/')">На главную</UiButton>
  </UiEmptyState>
</template>
