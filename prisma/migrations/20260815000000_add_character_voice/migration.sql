-- Голос ведущего переезжает с ролика на персонажа.
--
-- Video.voiceoverVoiceId заполнялся руками на каждый ролик, а пустое значение
-- уводило синтез на стоковый пресет спеки. При потоке в сотни роликов это
-- вопрос времени, когда на лицо конкретного человека ляжет чужой голос.
--
-- voiceModelId рядом не для симметрии: клон обучается ПОД конкретную модель
-- (minimax/voice-cloning принимает её параметром), и тот же voiceId в другой
-- модели не существует — платный запрос ушёл бы в никуда.
ALTER TABLE "Character" ADD COLUMN "voiceId" TEXT;
ALTER TABLE "Character" ADD COLUMN "voiceModelId" TEXT;
